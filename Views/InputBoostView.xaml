<UserControl x:Class="Frakture_Tweaks.InputBoostView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Frakture_Tweaks"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Margin="30">
            <TextBlock Text="{DynamicResource Input_Title}" 
                       Foreground="White" 
                       FontSize="24" 
                       FontWeight="SemiBold" 
                       Margin="0,0,0,20"/>

            <TextBlock Text="{DynamicResource Input_Desc}" 
                       Foreground="#BBB" 
                       FontSize="14" 
                       Margin="0,0,0,30"
                       TextWrapping="Wrap"/>

            <TextBlock Text="{DynamicResource Input_OptTitle}" FontSize="16" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,10"/>
            <TextBlock Text="{DynamicResource Input_Opt1}" Foreground="#BBB" Margin="5,0,0,5"/>
            <TextBlock Text="{DynamicResource Input_Opt2}" Foreground="#BBB" Margin="5,0,0,5"/>
            <TextBlock Text="{DynamicResource Input_Opt3}" Foreground="#BBB" Margin="5,0,0,5"/>
            <TextBlock Text="{DynamicResource Input_Opt4}" Foreground="#BBB" Margin="5,0,0,5"/>
            <TextBlock Text="{DynamicResource Input_Opt5}" Foreground="#BBB" Margin="5,0,0,20"/>

            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                <Button x:Name="BtnApply"
                        Content="{DynamicResource Input_Apply}"
                        Click="BtnApply_Click"
                        Background="#333"
                        Foreground="White"
                        FontSize="14"
                        Padding="17,10"
                        BorderThickness="0"
                        Cursor="Hand"
                        Width="240"
                        Margin="0,0,15,0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </Button.Resources>
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" CornerRadius="4">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#444"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Button x:Name="BtnRevert"
                        Content="{DynamicResource Input_Revert}"
                        Click="BtnRevert_Click"
                        Background="#333"
                        Foreground="White"
                        FontSize="14"
                        Padding="17,10"
                        BorderThickness="0"
                        Cursor="Hand"
                        Width="240">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </Button.Resources>
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" CornerRadius="4">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#444"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>