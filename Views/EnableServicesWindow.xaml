<Window x:Class="Frakture_Tweaks.EnableServicesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Frakture_Tweaks"
        mc:Ignorable="d"
        Title="{DynamicResource EnableSvc_Title}" Height="450" Width="700"
        Background="#1F1F1F"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid>
        <Grid.Resources>
            <Style x:Key="ServiceRadioButtonStyle" TargetType="RadioButton">
                <Setter Property="Background" Value="#2D2D30"/>
                <Setter Property="Foreground" Value="#CCCCCC"/>
                <Setter Property="FontSize" Value="16"/>
                <Setter Property="Padding" Value="20,15"/>
                <Setter Property="Margin" Value="0,0,0,20"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="HorizontalAlignment" Value="Stretch"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="RadioButton">
                            <Border x:Name="Border" 
                                    Background="{TemplateBinding Background}" 
                                    BorderBrush="#444" 
                                    BorderThickness="1" 
                                    CornerRadius="6">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <StackPanel Grid.Column="0" Orientation="Vertical" Margin="{TemplateBinding Padding}">
                                        <TextBlock Text="{TemplateBinding Content}" 
                                                   FontWeight="SemiBold"
                                                   FontSize="16"
                                                   Foreground="{TemplateBinding Foreground}"/>
                                        <TextBlock Text="{Binding Tag, RelativeSource={RelativeSource TemplatedParent}}" 
                                                   FontSize="12" 
                                                   Foreground="#888" 
                                                   Margin="0,4,0,0"
                                                   TextWrapping="Wrap"/>
                                    </StackPanel>
                                    
                                    
                                    <Border x:Name="Indicator" 
                                            Grid.Column="1"
                                            Width="10" Height="10" 
                                            Background="#999" 
                                            CornerRadius="5" 
                                            Margin="0,0,15,0" 
                                            Visibility="Collapsed"/>
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Border" Property="Background" Value="#383838"/>
                                    <Setter TargetName="Border" Property="BorderBrush" Value="#666"/>
                                </Trigger>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="Border" Property="Background" Value="#252526"/>
                                    <Setter TargetName="Border" Property="BorderBrush" Value="#999"/>
                                    <Setter Property="Foreground" Value="White"/>
                                    <Setter TargetName="Indicator" Property="Visibility" Value="Visible"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </Grid.Resources>

        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="{DynamicResource EnableSvc_Header}" 
                       Grid.Row="0"
                       Foreground="#FFF" 
                       FontSize="24" 
                       FontWeight="Bold" 
                       Margin="0,0,0,25" 
                       HorizontalAlignment="Center"/>

            <StackPanel Grid.Row="1" VerticalAlignment="Center">
                <RadioButton x:Name="AllServicesRadio"
                             Content="{DynamicResource EnableSvc_All}" 
                             Tag="{DynamicResource EnableSvc_AllDesc}"
                             GroupName="ServiceOption" 
                             Style="{StaticResource ServiceRadioButtonStyle}"
                             IsChecked="True"/>
                             
                <RadioButton x:Name="SelectExactRadio"
                             Content="{DynamicResource EnableSvc_Exact}" 
                             Tag="{DynamicResource EnableSvc_ExactDesc}"
                             GroupName="ServiceOption" 
                             Style="{StaticResource ServiceRadioButtonStyle}"
                             Margin="0"/>
            </StackPanel>

            <Button x:Name="ContinueBtn"
                    Grid.Row="2"
                    Content="{DynamicResource EnableSvc_Continue}"
                    Click="ContinueBtn_Click"
                    Background="#333"
                    Foreground="White"
                    FontSize="14"
                    Padding="40,12"
                    BorderThickness="0"
                    HorizontalAlignment="Center"
                    Cursor="Hand"
                    Margin="0,25,0,0">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="4"/>
                    </Style>
                </Button.Resources>
            </Button>
        </Grid>
    </Grid>
</Window>